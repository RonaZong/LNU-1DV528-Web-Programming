import styled from "react-emotion";
import { props } from "ramda";

const ExerciseSolutionLink = (props) => <a class="exercise-solution" href={props.link}>Lösningsförslag</a>

export default ExerciseSolutionLink;
