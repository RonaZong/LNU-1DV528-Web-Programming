/**
 * A included file
 *
 */

/**
 * Some function to show off how it works.
 *
 * @param {number} num1 A number to add.
 * @param {number} num2 Another number to add.
 * @returns {number} The sum of the parameters.
 */
function add (num1, num2) {
  return num1 + num2
}
